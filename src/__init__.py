"""
src package — root of the candidate_transformer source tree.

This package re-exports the primary public API surface for convenience.
Internal modules should use direct package imports (e.g.
``from src.models import CandidateProfile``) for clarity.
"""
