# Multi-Source Candidate Data Transformer — Python dependencies
#
# Core runtime
pydantic[email]>=2.7,<3.0   # Data validation and settings management (v2 API)

# Phase 2 runtime
pdfplumber>=0.11             # PDF text extraction (ResumAdapter)

# Phase 3+ (uncomment as features are implemented)
# phonenumbers>=8.13         # E.164 phone number normalisation
# PyYAML>=6.0                # YAML config file loading
# httpx>=0.27                # Async HTTP client for ATS API sources

# Development / testing
pytest>=8.0
pytest-cov>=5.0
reportlab>=4.0               # Programmatic PDF generation for test fixtures

streamlit>=1.30.0
